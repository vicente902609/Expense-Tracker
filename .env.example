# =============================================================================
# Root (optional) — copy to `.env` in this directory if you want shared defaults
# =============================================================================
# Optional: duplicate OpenAI defaults for tooling that reads the repo root.
# Primary API keys belong in packages/backend/.env for the server.
#
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini
